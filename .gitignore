_ai/
tmp*/
logs/
.worktrees/

# Local model/runtime caches must stay outside git.
models/
model-cache/
.cache/
hf-cache/
checkpoints/

# Go build artifacts (the zoovoice binary is built locally, never committed).
services/zoovoice/zoovoice

# Python / Node build artifacts for the future web app.
__pycache__/
.pytest_cache/
*.egg-info/
.venv/
venv/
node_modules/
test-results/
dist/
build/
.env
.env.*
!.env.example
.runpod.env
.dev.vars
.wrangler/
_ai_output/

# Terraform: stateとproviderキャッシュはローカル専用（リモートbackend移行までの暫定）
infra/**/.terraform/
infra/**/terraform.tfstate
infra/**/terraform.tfstate.backup
infra/**/*.tfplan

# Terraform: 個人アカウント等の実値。変数の宣言だけをリポジトリに置く
infra/**/*.tfvars
infra/**/*.tfvars.json
