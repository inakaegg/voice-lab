# voice-lab のagent-kit設定（正本の既定値は agent-kit/agent-settings.env）。
# ここにはkit既定から変える値、または明文化しておきたい値だけを書く。

# commit件名・本文の言語順。kit既定と同じ en-ja（English / 日本語）を明示的に採用する。
# 2026-09-01 以前の履歴には ja-en 混在があるが、以後は en-ja に揃える（AGENTS.md「Git運用」も同順へ更新済み）。
COMMIT_LANG_ORDER=en-ja
